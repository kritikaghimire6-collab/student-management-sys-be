export enum RoleEnum {
  ADMIN = "admin",
  FACULTY = "faculty",
  STUDENT = "student",
  PARENT = "parent",
}

export enum AttendanceStatusEnum {
  PRESENT = "present",
  ABSENT = "absent",
  LEAVE = "leave",
}
